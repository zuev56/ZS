services:

  worker:
    build:
      context: ./Vk/Zs.VkActivity.Worker
    restart: always

  web-api:
    build:
      context: ./Vk/Zs.VkActivity.WebApi
    ports:
      - "6011:80"
    restart: always
